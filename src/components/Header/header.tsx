const Header = () => {
  return (
    <div className="h-[70px] bg-white border-b border-gray-7 flex items-center justify-center">
      <span className="text-lg font-semibold">Header (70px)</span>
    </div>
  );
};

export default Header;
